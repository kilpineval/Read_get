# Leer_obtener
Leeremos un archivo
#include<stdio.h>
#include<conio.h>
int main(){
  FILE * ptr;
  matriz de caracteres[50]=(lectura, rt);
  if(ptr=(fopenArchivo)){
    printf(ptr = ("El archivo no se abrio por alguna razon");
    
  }
    imprimirf("El archivo no
}
devolver0;
